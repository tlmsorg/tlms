package com.sys.controller;

import org.springframework.web.bind.annotation.RestController;

/**
 * 待办任务消息推送测试
 * @author tom
 *
 */
@RestController
public class PjMessageTestController {
	/*@Autowired
	private IPjMessageService pjMessageServiceImpl;
	
	@ResponseBody
	@RequestMapping(value="/push/{accountId}/{sendChannelFlag}/{msg}/{tranCode}")
	public void pushMessage(@PathVariable String accountId,@PathVariable String sendChannelFlag,@PathVariable String msg,@PathVariable String tranCode) throws Exception{
		System.out.println("PjMessageTestController--->pushMessage");
		pjMessageServiceImpl.pushMessage(accountId, sendChannelFlag, msg,tranCode);
	}
	*/
	
}
